# 引き継ぎ

## はじめに読む人へ（この会話を知らない Claude 向け）

**2 つのリポジトリが対になっています。**

| | |
|---|---|
| [jwcad_dos_wasm](https://github.com/yomei-o/jwcad_dos_wasm) | ＪＷ＿ＣＡＤ for DOS/V を逆コンパイルして C に書き直す「移植」。ブラウザで動きます |
| **このリポジトリ** | その移植が正しいかを確かめるための「本物」。DOS/V エミュレータで `JW_CADV.EXE` をそのまま動かします |

**最終目標**は、同じ操作をしたときの画面を 2 つで突き合わせて、
移植が本物と同じ絵を出すことを確かめることです。移植側には
「ネイティブ対 WASM を 1 画素ずつ比べる」仕組み（`tools/check.sh`）が
すでにあり、その相手をこのエミュレータに置き換えるのが最後の一歩です。

**この 2 つは同じフォントファイルを使います。** DOS/V のプログラムは
字形を持たず OS に尋ねるので（`INT 15h AX=5000h`）、エミュレータが
`jwcad_dos_wasm/font/*.FNT` を返し、移植側も同じものを読みます。
字形が違えば画面比較に意味がありません。

**同じパレットも使います。** JW_CAD は `JW_PAL.DAT` を読んで
`INT 10h AX=1010h` で 16 色を自分で入れ直します。EGA の既定とは別物で、
4 は赤ではなく緑、6 は茶ではなく黄、7 は明灰ではなく白です。
どちらも実装済みです（こちらは `INT 10h AH=10h`、移植側は
`jw_view_palette()`）。**6 ビットから 8 ビットへの変換規則も揃えてあります**
——上位ビットの複製（`(v<<2)|(v>>4)`）。片方だけ変えると、画素が全部合っていても
色で食い違います。

両方を並べて置いてください。

```
どこか/
  jwcad_dos_wasm/     移植
  dosv_emu_cpp/       これ
```

必要なのは C++17 のコンパイラだけです（`sh build.sh`）。
このマシンには PATH が通った gcc がないので `w64devkit` を使います。

```sh
export PATH="/c/prog/tools/w64devkit/bin:$PATH"
sh build.sh
```

### 引き継ぎ（2026-09-18 朝）

**`INT 16h AH=12h`（拡張シフト状態）を answer するようにしました**（`f5da352`）。
素通りさせていたので AX が呼び出し時のまま帰り、JW_CAD は
呼び出し側の `union REGS` に残っていた値——Ctrl のビットが立った `0x94`——を
シフト状態として読んでいました。**対の repo（jwcad_dos_wasm）の測定が
全部「Ctrl を押しながら」になっていた**のはこれです。向こうの RESUME の
「引き継ぎ」を先に読んでください。

* `Dos::set_mods()` と BIOS の 0040:0017/0018 を持たせ、`AH=02h`/`AH=12h` を
  そこから答えます
* 台本に **`mods shift` / `mods ctrl` / `mods alt`（`grph` も同じ）/ `mods none`**
  を足しました。読取の修飾キーはここから測れます
* **建て直しが要ります**: `PATH="/c/prog/tools/w64devkit/bin:$PATH" sh build.sh`
  （`build.sh` は `g++` を探すだけなので、PATH に w64devkit を入れること）

**次に見るとよさそうなもの**: `INT 33h` のイベントハンドラ（`AH=0Ch`）は
登録を覚えるだけで**呼んでいません**。JW_CAD は使っていない（関数 0/1/2/3/4/5/7/8/9
しか呼ばない）ので急ぎませんが、ほかのプログラムでは効きます。

## いまどこまで

**`JW_CADV.EXE` が起動して、メニューを描いて、マウスとキーに応えて、
図面を読んで描きます。**

```sh
mkdir -p tmp
./dosemu --root ../jwcad_dos_wasm/orig \
         --font-ank  ../jwcad_dos_wasm/font/JWANK16.FNT \
         --font-kanji ../jwcad_dos_wasm/font/JWKAN16.FNT \
         --script scripts/drawing.txt \
         ../jwcad_dos_wasm/orig/JW_CADV.EXE SAMPLE1.JWC
```

`tmp/drawing.png` に木造平面図が出ます。左上に要素数（線 762 / 円・文字 24）、
左下にレイヤのボタン、ステータス行に `S=1/100` `A-4` `表示倍率 0.57`。
`SAMPLE2.JWC` ならマンションの基準階平面図。

`scripts/startup.txt` は起動画面（2000 万命令で落ち着きます）と、
マウスを動かして左ボタンを押したところ——上段メニュー
「1)ﾌｧｲﾙ(L) 2)ﾌﾟﾛｯﾀ(R) 3)ﾌﾟﾘﾝﾀ 4)自動保存(無) 5)新規図面 6)終了」——の 3 枚。

メニューからファイルを開くこともできます（左クリック → `1` → `2` で
ファイル選択が出て、`A:\*.jwc` の 15 枚が題名つきで並びます）。

**これで画面の突き合わせに必要なものは揃いました。** 残っているのは
比較そのもの（と WASM 版）です。

### 日本語入力 —— 本物の FEP が JW_CAD の上で動きます

一九九四年のかな漢字変換ドライバ**「鳳」(おおとり)** を組み込んで、
JW_CAD の文字列入力で変換できます。経緯と仕組みは `fep/README.md` に、
ここには走らせ方だけ。

```sh
python tools/mksmsdic.py fep/SKK-JISYO.L tmp/jwroot   # 辞書（一度だけ）
python ../jwcad_dos_wasm/tools/lzh.py fep/otri055.lzh tmp/otri/   # OTRI.SYS を出す
./dosemu --root tmp/jwroot --device "tmp/jwroot/OTRI.SYS /dA:\\" \
         --font-ank ... --font-kanji ... tmp/jwroot/JW_CADV.EXE SAMPLE0.JWC
```

`SMSREA.DIC` `SMSDIC.DIC`（作るもの）と `SMSROM.DIC` `SMSTXT.ST`
（`otri055.lzh` に入っているもの）を `--root` の下に置いてください。

```
変換 → 「鳳」起動（右下に「鳳」）→ yama → 入力行に「やま」→ SPACE →
仮想鍵盤に「山」「岾」→ 1 → JW_CAD の文字列に「山」→ Enter → 基点指示
```

スクリプトでは `key xB200`（変換キーの BIOS キー語）で起こします。
`tmp/otri8.txt` `tmp/otri12.txt` がその並びです（`tmp/` は git 外）。

**JW_CAD は「鳳」にも WXP にも対応していません。それでも通ります。**
検出テーブル（`DS:0x1514` の `FP$WXP` `FP$ATOK6` …）は FEP を名前で開いて
on/off を送るためのもので、文字の通り道ではないからです。通り道は
`INT 21h AH=07h`＝CON ドライバへの READ 要求で、CON になりすました FEP は
その中で変換を済ませ、確定した文字だけを返します。対応が省くのは
**変換キーを自分で押す手間**だけです。

**変換窓がどこに出るかはアプリが決めています。** 「鳳」のインライン入力は
`INT 10h AH=03h` でカーソル位置を聞き、その**行**の桁 0 から書きます
（`clr dl`）。JW_CAD は画面に出さないだけでカーソルを持っていて、
`AH=02h` で `0100h` を置いていました——だから「やま」は 1 行目、
メニューの上に出ます。`DOSEMU_TEXT_TRACE=1` で
`[text] cursor <- 0100` / `[text] cursor -> 0100` として見えます。

## 操作を流し込む —— `--script`

窓のないグラフィックアプリには「いま」がありません。
画面比較は**操作の並び**を両側で同じに再生できて初めて成り立つので、
走らせ方をスクリプトで書けるようにしてあります。

```
# scripts/startup.txt
wait 20000000        # 命令数。秒ではない
mouse 300 200
click L
key esc
type ABC
shot tmp/01.png
```

| | |
|---|---|
| `wait N` / `run N` | N 命令ぶん走らせる |
| `mouse X Y` | カーソルを動かす（モード 12h では画素座標そのもの） |
| `down L\|R\|M` / `up ...` / `click ...` | ボタン |
| `key NAME` | `enter` `esc` `tab` `bs` `space` `up` `down` `left` `right` `home` `end` `pgup` `pgdn` `ins` `del` `f1`〜`f12` `xfer` `nfer`、1 文字、`xHH`（生のバイト）、`xHHHH`（BIOS のキー語そのもの。`xB200` が DOS/V の 変換） |
| `type TEXT` | 行の残りを 1 文字ずつ |
| `mods NAMES` | ここから先ずっと押さえている修飾キー。`shift` `ctrl` `alt`（`grph` も同じ）`rshift` をカンマで並べる。`mods none` で離す |
| `shot PATH` | いま画面を PNG に |
| `dump SEG:OFF N` | ゲストのメモリを N バイト（既定 16）。変数の中身を見る |
| `end` | そこで止める |

ほかに `--device "PATH ARGS"`（CONFIG.SYS の DEVICE= 相当、繰り返せる）と
`--run "PROG ARGS"`（AUTOEXEC.BAT 相当。`AH=31h` で終われば常駐する）。

**時計を命令数にしてあるのは意図的です。** 実時間だとマシンの混み具合で
2 つの走行の位置がずれるし、合わせる先がありません。**そして「命令数」は
外側のループの回数ではありません**——コンソール読み出しをデバイスドライバが
答えると、その 1 回の中で何百万命令も走ります。スクリプトは命令数の時刻つきの
並びとして走り、エミュレータが自分で命令をまわす場所（`call_far`）からも
同じ pump を呼びます。これが無いと、FEP がキーを待ち始めたところで
スクリプトが止まります。

## 次にやること

### 1. 画面の突き合わせ（これが目的）

両側に必要なものは揃いました。

* **同じ字形** —— `--font-ank` / `--font-kanji` で移植側と同じ `font/*.FNT`
* **同じ色** —— こちらは `INT 10h AX=1010h` を実装済み、移植側は
  `jw_view_palette()` で `JW_PAL.DAT` を読む。6 ビット → 8 ビットの
  変換規則も揃えてあります（`(v<<2)|(v>>4)`）
* **同じ操作** —— `--script` の並びを両側で再生する

**そして回り始めました。** 移植は原典と同じ場所に、同じ色で、同じ画素に
線を置きます（SAMPLE1 で 7,388 画素一致、差 1.37%）。
`shot` の名前を `.raw` にすると 640×480 の RGBA で書くので、
向こうの `tests/compare.exe` がそのまま差分の地図を出します。

決め方はどれも**こちらの画面から測る**でした。

* `DOSEMU_BPPTR` には**レジスタ名**も書ける（`DOSEMU_BPPTR=ax`）。
  JW_CAD の整数三角関数は AX で返すので、「この半径とこの角度で本物は
  何を返すか」を 636 個読むのに要った——それで円弧を折れ線にする式が
  1 つに決まっている（jwcad_dos_wasm の RESUME「円弧」）
* 座標変換 —— `DOSEMU_BP=0EFF:17BB DOSEMU_BPPTR=2,3,4,5` で、本物が
  線ごとに計算した画面座標（float）を出し、`.JWC` の同じ線と突き合わせる。
  答えは `screen_x = (int)(x + 121)`、`screen_y = (int)(463 - y)`、**倍率 1**
* ペン → 色 —— 線に沿って画面の色を拾って集計する
  （`jwcad_dos_wasm/tools/measure.py` がやります）。`.JWC` の 2 バイトは
  「線種、ペン」の順で、移植は逆に読んでいた
* 文字 —— **原典は字形を描かず、枠の矩形を描いていた**

残っていることは向こうの RESUME.md に並べてあります
（文字の枠の高さ、レイヤの表示・非表示、本物の UI）。

### 2. WASM 版 —— **できました**（2026-09-20）

`sh tools/build_wasm.sh` が `dosemu.js` + `dosemu.wasm` を作り、`index.html` が
それを **Web Worker** の中で回します。GitHub Pages に置けばそのまま動きます。

構え方:

* `src/main_wasm.cpp` が 2 つ目のフロントエンド（`src/main.cpp` はネイティブ用の
  まま残す）。出しているのは `de_boot` / `de_run` / `de_frame` / `de_mouse` /
  `de_button` / `de_key` / `de_mods` と、様子を見るための `de_width` ほか
* **時計は worker が持ちます。** ゲストは本物のプログラムで、起動に 4000 万命令、
  押しに応えるのに 2000 万命令要ります。ページを滑らかに保てるほど細かく刻むと
  ゲストが進まないので、worker が回し続けて**画面が変わったときだけ**送ります
* 画面が変わったかは**全画素の比較**です。最初サンプリングした hash で済ませたら、
  30 万画素の中の数百画素の線を取りこぼして、絵が 1 枚出たきりになりました。
  全部見ても 1 ミリ秒ほどで、その前の slice に比べれば無視できます
* `Vga::rgba()` を足しました（`save_raw()` の、ファイルに書かない版）
* guest の一式（`JW_CADV.EXE`・図面 14 枚・フォント）は `--embed-file` で
  .wasm に焼いてあります。隣の `../jwcad_dos_wasm/orig` から取ります

**検査**: `sh tools/wasmcheck.sh` —— 同じ台本をネイティブとブラウザ版の両方に
流して 640x480 を `cmp` で比べます。起動画面・作図・読取・**日本語入力**の
4 件とも**1 バイトも違いません**。台本を WASM 側に流すのは
`tools/wasmshot.mjs`（`wait`/`mouse`/`down`/`up`/`click`/`key`/`type`/`shot`）。

速さ: ネイティブが 3200 万命令/秒、ブラウザ版が 1400 万命令/秒（node 実測）。
起動に 3 秒ほど、押しの応答に 1.5 秒ほどです。`-flto` は**遅くなった**ので
入れていません（9.5 秒 対 7.8 秒、.wasm も大きくなる）。

日本語は **OS の入力メソッド**を使います（移植側と同じ方針）。隠した `<input>`
に変換させ、確定した文字列を Shift-JIS にして 1 バイトずつ `de_key()` へ。
DOS/V の FEP がしているのも「2 バイトを普通のキー 2 つとして渡す」だけです。

### 3. EMS（`INT 67h`）

いまは実装していません。JW_CAD は EMS が無ければ conventional メモリで
動くので図面は読めていますが、ファイル選択が「65,535 bytes free」と
言うのはそのせいです。大きい図面を開くときに効いてきます。

### 4. カーソルが 2 つある（小さいが直すべき）

`INT 10h AH=02h/03h` は `cursor_` を読み書きし、`text_cursor()` /
`text_set_cursor()`（`AH=0Eh`、`AH=13h` の AL ビット0）は `0040:0050/0051` を
読み書きします。**同じものを 2 箇所に持っていて、食い違います。**
本物の BIOS はデータエリアの 1 箇所だけです。DOS/V の FEP は
`0040:0050` を直接読むものもあるので、`cursor_` を捨てて
データエリアに一本化すること。

### 5. WXP for J-3100 —— キーの出どころが分からない

`WXP.SYS` + `WXDP.SYS` は組み込まれ（辞書 65 万バイトごと）、
`wxpdosv.exe` も `--run` で常駐します。が、**WXP の CON READ は 0 を返します。**
`INT 16h AH=00/10` を呼ばず、BIOS キーバッファも読まず、`INT 09h` も
フックしない——どこからキーを取るつもりなのかが分かっていません。
逆アセンブルが要ります。`fep/local/`（git 外、転載不可）にあります。

### 6. 「鳳」の残り

* 単漢字以外（連文節）は「鳳」自身が持っていません。単漢字 FEP です。
* `POP_LINE` / `POP_SCREEN` は退避した行を書き戻しますが、こちらの
  テキスト画面は**BIOS を通った文字しか憶えていません**。JW_CAD が自分で
  描いた画素は空白で上書きされます。本物の DOS/V でも表示ドライバは
  テキスト面しか持たないので同じはずですが、確かめていません。
* 仮想鍵盤の選択キーは**スキャンコード**で引きます（`KEY_TO_SMS`）。
  いまは `1` が 1 番目に当たることだけ確かめてあります。

### 7. オーバーフローフラグの直しを本家へ

`src/cpu.cpp` のシフト・ローテートが OF を立てていなかったのは
[dos_emu_cpp](https://github.com/yomei-o/dos_emu_cpp) から引き継いだ
コードです。**向こうにも同じ穴があります。**
ソフトウェア浮動小数点を使うプログラムは軒並み踏むので、持ち帰ること。

## 刺された罠

### `INT 16h AH=12h` を answer していなかった —— JW_CAD がずっと Ctrl を押されたと思っていた

**症状**は JW_CAD 側にしか出ませんでした。複写／移動の `①ﾏｳｽ位置` で
置く位置を押すと、どこを押しても・どちらのボタンでも
「読取可能データ無」になって何も複写されない。探索そのものは当たっていて
（`[0ce2]` に実体番号 5 が入る）、それでも断られる。

**原因**は `INT 16h AH=12h`（拡張シフト状態）を素通りさせていたことです。
`ah == 0x02` だけ `AL=0` を返していて、`0x12` は `return true` に落ちるので
**AX が呼び出し時のまま**帰る。JW_CAD は Microsoft C の `int86()` 越しに
呼ぶので、帰ってきたのは呼び出し側の `union REGS` にたまたま残っていた
値——`0x94`、つまり **Ctrl のビットが立った状態**でした。

JW_CAD はこれを押しごとに読み、`and ax,0eh` で
{2 = Shift, 4 = Ctrl, 8 = Alt} に落として `[0c1e]` に入れます
（`+03bf:2489` あたり）。`[0c1e] == 2`（Ctrl）だと点を取る
`+11f2:34e9` は「修飾つきの読取」の枝に入り、そこは命令側が
`[0ce6]` を立てていないと必ず `+11f2:365a` の
「読取可能データ無」へ跳びます。押し方の問題ではなく、
**エミュレータが「Ctrl が押されている」と答えていた**のが全部でした。

直したもの: `Dos` に `kbd_flags_`/`kbd_flags2_`（BIOS の 0040:0017/0018）を
持たせ、`AH=02h` と `AH=12h` の両方をそこから答える。ホスト側は
`Dos::set_mods()`、スクリプトからは `mods` で押さえられます。
**答えを持っていない BIOS 呼び出しを素通りさせてはいけません**——
レジスタをそのまま返すのは「0 を返す」ではなく「呼び出し側のごみを返す」です。

**`INT 21h` の読み書きは 64KB をまたぐ。** `AH=3Fh` / `AH=40h` の転送先を
`mem_.rb(seg, off + i)` で当てていました。**オフセットが 16 ビットなので、
64KB を越えたところでセグメントの頭に巻き戻ります。** 実機の DOS は DS:DX を
物理番地に直してそこから数えるだけなので、越えても続きます。`Memory::phys()`
で先に物理番地を作り、そこから 1MB でマスクして数えるように直しました。

見つけるのに半年かかったようなバグです。JW_CAD は TEST7.JWC（98,813 バイト）
の線の配列を 0x4000 ずつ読み書きするので、そのうち 1 回が必ず 64KB をまたぎ、
**読みでは配列の後ろ 1/3 が別のメモリに化けて**、画面から細かい影の線が
95 画素ぶん消えていました。それを長いあいだ「JW_CAD 側の 64KB バグ」だと
思って jwcad_dos_wasm の RESUME に書いてありました。分かったのは書きのほう
からで、本物に保存させたファイルの途中（`4612:CFA2` から 0x4000 バイトの
12,382 バイト目、`0xCFA2 + 12382 = 0x10000` ちょうど）に `"15.000"` という
別の場所の ASCII が混ざっていたのが尻尾でした。

**エミュレータを疑うこと。**「本物のバグ」と書く前に、まず自分を疑う。

**デバイスヘッダを INIT の前に読まない。** `OTRI.SYS` は DIET 圧縮で、
ディスク上のオフセット 0 にあるのは展開ルーチンのヘッダです。属性語
（`8013h`）は本物のまま——でないと DOS が組み込まない——ですが、**名前欄は
展開ルーチンのコードで潰れています**（`U..WVR` と読めた）。INIT が「鳳」を
上に展開して、はじめてヘッダが `CON` と言う。名前で CON を判定していたので
`con_driver_` が立たず、JW_CAD の `AH=07h` は FEP を素通りして、キーは
「鳳」の中に消えたまま**何のエラーも出ませんでした**。属性のビット0
（標準入力装置）でも CON を主張できるようにしてあります——DOS が実際に
見ているのはそっちです。

**`INT 28h` を「何もしない」にしない。** 常駐プログラムが割り込みの中で
できない仕事を置く場所です。「鳳」は SPACE を受け取るとフラグを立てて帰る
だけで、辞書を引いて仮想鍵盤を描くのは `INT 28h` の中。DOS は
`AH=0Bh`（文字が待っているか）の答えが「いいえ」のときにこれを上げます。
無いと SPACE は**無反応**で、しかもエラーは出ません。

**`INT 10h AH=13h` は書くだけの呼び出しではありません。** DOS/V では
AL のビット4 が「画面から ES:BP へ**読む**」、ビット5 が「文字と属性が交互」。
ビット5 を見落として文字だけの列として読んだので、`96h 07h 50h 07h`
（「鳳」＝ SJIS 9650h ＋属性）が `9607h` `5007h` という無い文字になり、
**背景色で塗っただけの真っ黒な窓**が出ました。読む側が無いと、FEP は
下にあった行を戻せないので窓が開いたきりになります。

**辞書の形式はドキュメントではなく探索コードで確かめる。** `CONVDIC.C` の
書き方を JIS コードと読んだが、実物（`search_rea2`）は `or dx,080h` で
1 バイト目のビット7 を立て直してから文字として使う。ビット7 が立っているのは
**Shift-JIS の 1 バイト目**だけです。しかもそのビットは鍵盤位置の第 9 ビット
「そのもの」ではなく**補数**として読まれる（`not bh / rol bh,1 / and bh,1`）。
辞書は作れているのに候補が 0 件、という出方をします。

**フラグを「誰も見ていないだろう」で省かない。** シフトとローテートが
OF を一切立てていなかった。踏むのはこの一行です。

```asm
    shl dx, 1
    rcl bx, 1
    jno -            ; 最上位ビットが立つまで回す
```

仮数の正規化はこう書くのが定石で、Microsoft C のソフトウェア浮動小数点
パッケージもそう書いてある。OF が立たないとループが正しい位置で止まらず、
**整数から浮動小数点への変換がいつでも 1.0 になる**。JW_CAD では
それがクリップ枠（DGROUP 0xb5aa/0xb5b2/0xb60e/0xb612 に浮動小数点で入る）を
点 (1.0, 1.0) にして、762 本の線を全部捨てていた——読み込みは成功し、
実体ループは最後まで回り、画面は真っ黒。**どこにも「失敗した」と出ない。**

ローテートが SF/ZF/PF を書き換えていたのも同じ穴の裏側で、
これを触っていいのはシフトだけ。ローテートで書き換えると、呼び出し元が
直前にやった比較の結果が消える。

**「暴走している」を信じない。まず最後まで走らせる。** 前回は
16 万命令でフォント取得の far call がゼロを実行している、と書いてあった。
実際には**660 万命令まで走りきって**、`INT 33h` 機能 0（リセット）が
AX=0 を返したので「ﾏｳｽﾄﾞﾗｲﾊﾞｰが組み込まれていません」と出して
`exit(1)` していた。JW_CAD はマウスの有無を訊いているのではなく、
**要求している**。オーバーレイ 8 のリンク `2ab8:3132`。

**「文字が待っている」と嘘をつかない。** `INT 21h AH=0Bh` が
0xFF 固定だった。JW_CAD のキー待ちは AH=0Bh が「はい」と言ったときだけ
AH=08h で読むので、いつでも「はい」だと永久に読み続ける。
何も打ち込まない走行では 700 万命令のところで画面が止まり、
そのあと 6000 万命令走らせても 1 画素も変わらなかった。
`INT 16h AH=01h` で同じ嘘を避けてあったのに、DOS 側が残っていた。
**入力の「待っている？」は全部同じ `input_ready` フックに繋ぐこと。**

**パレットを受けて無視しない。** JW_CAD は `JW_PAL.DAT` を読んで
`INT 10h AX=1010h` で DAC 16 本、`AX=1000h` でアトリビュートパレットを
恒等に設定する。無視すると絵は合っているのに色だけ EGA の既定になり、
**画面比較としては「違う絵」**になる。ポート書き込みを全部並べて
（`DOSEMU_IO_TRACE=1`）0x3CE/0x3CF しか無いと分かったのが手がかりだった。

**BIOS データエリアを空のままにしない。** 中立ではありません。
JW_CAD は画面の横幅を `0040:004A`、文字セルの高さを `0040:0085` から取って
**割り算します**。0 のままだと `R6003 - integer divide by 0` で、
何も描かずに止まります。しかも Microsoft C のランタイムが出すメッセージなので、
`DIV` 命令を捕まえても引っかかりません。

**`INT 10h` を「受けて無視」にしない。** `AH=0Fh` でモードを取って
終了時に戻すプログラムは多く、返り値が無いと変な値を覚えます。

**DOS/V のプログラムは字形を持っていない。** `INT 15h AX=5000h` で
「字形を写すルーチン」のアドレスを尋ね、それを far call します。
実装が無いと関数ポインタがゴミのままで、最初の 1 文字で落ちます。
これは **`cd 15` をバイト列で探しても見つかりません**——DOS/V のプログラムは
たいてい `int86()` 経由で呼ぶので、機械語に `INT` 命令が現れないからです。

**低位メモリに置いたものは消える前提で。** `INT 15h AX=5000h` が返す
far ポインタの先に 3 バイトのスタブを置いたが、`0090:0000` では
（起動時に書いても、`INT 15h` の直前に書き直しても）ゲストが使うころには
ゼロになっていた。ゼロは `add [bx+si], al` なので、CPU は静かに
前へ進み続ける。いまは `F000:FF00`（ROM BIOS 領域）に置いてある。

**フォントは外から渡せるようにしておく。** 移植の検証に使うなら、
**両方がまったく同じ字形を描かなければ比較に意味がありません。**
だから `--font-ank` / `--font-kanji` で FONTX2 を渡す形にしてあります。

## 使えるトレース

```sh
DOSEMU_DOS_TRACE=1      # INT 21h を 1 本ずつ（開いたファイル名も出る）
DOSEMU_TRACE=lo-hi      # 命令番号の範囲で 1 命令ずつ（2 万命令で数分。範囲は狭く）
DOSEMU_SAMPLE=n         # n 命令ごとに CS:IP。「どこにいるか」を数秒で
DOSEMU_WATCH=lo-hi      # 物理番地への読み書き（VRAM は見ない。**標準出力**に出る）
DOSEMU_BP=seg:off[,..]  # その番地に来るたびにスタック 10 語 ＝ C の引数表
DOSEMU_BPSTR=N          # そのうち N 番目を DS 相対の文字列として出す
DOSEMU_FONT_TRACE=1     # 字形要求（文字コードと格納先）
DOSEMU_IO_TRACE=1       # ポート書き込みと INT 10h
DOSEMU_MOUSE_TRACE=1    # INT 33h
DOSEMU_VRAM_MAP=1       # 各プレーンの立っているビットが見えるページの中か外か
DOSEMU_FPU_TRACE=1      # x87 エスケープ（JW_CAD では 0 回）
DOSEMU_TEXT_TRACE=1     # INT 10h のテキスト側。FEP が何をどこに書いたか＋カーソル
DOSEMU_KBD_TRACE=1      # BIOS キーバッファの出し入れ（誰が取ったか）
DOSEMU_CON_TRACE=1      # CON ドライバへの READ / 非破壊 READ と、その答え
```

**FEP を追うときはこの 3 つを一緒に。** キーが消えたときに知りたいのは
「アプリと FEP のどちらが取ったか」で、それは `[kbd]` の呼び出し元 CS:IP に
出ます。窓が出ないときに知りたいのは「書かなかったのか、別の場所に書いたのか」で、
それは `[text]` に出ます。

**グラフィックスを追うときに要るのは「どこにいるか」でも
「何を計算したか」でもなく「誰がどの座標で呼んだか」です。**
`DOSEMU_BP` はそのために足しました。たとえば文字列描画なら:

```sh
DOSEMU_BP=0EFF:23C5 DOSEMU_BPSTR=2 ./dosemu ... 2>/dev/null | grep '^\[bp\]'
# -> args <戻り番地> <文字列> <桁> <行> <色> <モード> ... "その文字列"
```

## 押さえた番地（JW_CAD 側）

ロード基底は `0x110` です（DGROUP が `3485`、これは
[jwcad_dos_wasm](https://github.com/yomei-o/jwcad_dos_wasm) の
リンク時アドレス `3375` ＋ `0x110`）。トレースを読むときの換算に使います。
**`jwcad_dos_wasm/tools/disasm.py` はリンク時アドレスを取るので、
エミュレータで見た CS から `0x110` を引いてから渡してください。**

| エミュレータの CS | リンク | 中身 |
|---|---|---|
| `0110` | `0000` | `main` のある最初のコードセグメント |
| `0995` | `0885` | ステータス行・メニューまわり |
| `0EFF` | `0def` | コンソール層（`0def:23c5` 文字列、`0def:1460` 矩形、`0def:12e8` クリップ枠、`0def:17bb` クリップつきの線） |
| `11B9` | `10a9` | 描画（`10a9:03e1` が 1 文字、`10a9:07dc` が線、`10a9:0732` が色） |
| `1302` | `11f2` | 再描画（`11f2:27eb` が図面の描き直し、`11f2:0680` が実体ごとの表示判定） |
| `23C2` | `22b2` | Microsoft C のランタイム（`22b2:247a` が `int86`、`22b2:70xx` が浮動小数点） |
| `2B85` | `2a75` | Shift-JIS / JIS 変換 |
| `2BC8` | `2ab8` | オーバーレイが載る穴 |
| `3485` | `3375` | DGROUP |

JW_CAD 側の構造・オーバーレイ・ファイル形式は
[jwcad_dos_wasm の RESUME.md](https://github.com/yomei-o/jwcad_dos_wasm/blob/main/RESUME.md)
に全部書いてあります。

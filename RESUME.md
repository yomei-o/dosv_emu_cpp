# 引き継ぎ

## はじめに読む人へ（この会話を知らない Claude 向け）

**2 つのリポジトリが対になっています。**

| | |
|---|---|
| [jwcad_dos_wasm](https://github.com/yomei-o/jwcad_dos_wasm) | ＪＷ＿ＣＡＤ for DOS/V を逆コンパイルして C に書き直す「移植」。ブラウザで動きます |
| **このリポジトリ** | その移植が正しいかを確かめるための「本物」。DOS/V エミュレータで `JW_CADV.EXE` をそのまま動かします |

**最終目標**は、同じ操作をしたときの画面を 2 つで突き合わせて、
移植が本物と同じ絵を出すことを確かめることです。移植側には
「ネイティブ対 WASM を 1 画素ずつ比べる」仕組み（`tools/check.sh`）が
すでにあり、その相手をこのエミュレータに置き換えるのが最後の一歩です。

**この 2 つは同じフォントファイルを使います。** DOS/V のプログラムは
字形を持たず OS に尋ねるので（`INT 15h AX=5000h`）、エミュレータが
`jwcad_dos_wasm/font/*.FNT` を返し、移植側も同じものを読みます。
字形が違えば画面比較に意味がありません。

**同じパレットも使います。** JW_CAD は `JW_PAL.DAT` を読んで
`INT 10h AX=1010h` で 16 色を自分で入れ直します。EGA の既定とは別物で、
4 は赤ではなく緑、6 は茶ではなく黄、7 は明灰ではなく白です。
どちらも実装済みです（こちらは `INT 10h AH=10h`、移植側は
`jw_view_palette()`）。**6 ビットから 8 ビットへの変換規則も揃えてあります**
——上位ビットの複製（`(v<<2)|(v>>4)`）。片方だけ変えると、画素が全部合っていても
色で食い違います。

両方を並べて置いてください。

```
どこか/
  jwcad_dos_wasm/     移植
  dosv_emu_cpp/       これ
```

必要なのは C++17 のコンパイラだけです（`sh build.sh`）。
このマシンには PATH が通った gcc がないので `w64devkit` を使います。

```sh
export PATH="/c/prog/tools/w64devkit/bin:$PATH"
sh build.sh
```

## いまどこまで

**`JW_CADV.EXE` が起動して、メニューを描いて、マウスとキーに応えて、
図面を読んで描きます。**

```sh
mkdir -p tmp
./dosemu --root ../jwcad_dos_wasm/orig \
         --font-ank  ../jwcad_dos_wasm/font/JWANK16.FNT \
         --font-kanji ../jwcad_dos_wasm/font/JWKAN16.FNT \
         --script scripts/drawing.txt \
         ../jwcad_dos_wasm/orig/JW_CADV.EXE SAMPLE1.JWC
```

`tmp/drawing.png` に木造平面図が出ます。左上に要素数（線 762 / 円・文字 24）、
左下にレイヤのボタン、ステータス行に `S=1/100` `A-4` `表示倍率 0.57`。
`SAMPLE2.JWC` ならマンションの基準階平面図。

`scripts/startup.txt` は起動画面（2000 万命令で落ち着きます）と、
マウスを動かして左ボタンを押したところ——上段メニュー
「1)ﾌｧｲﾙ(L) 2)ﾌﾟﾛｯﾀ(R) 3)ﾌﾟﾘﾝﾀ 4)自動保存(無) 5)新規図面 6)終了」——の 3 枚。

メニューからファイルを開くこともできます（左クリック → `1` → `2` で
ファイル選択が出て、`A:\*.jwc` の 15 枚が題名つきで並びます）。

**これで画面の突き合わせに必要なものは揃いました。** 残っているのは
比較そのもの（と WASM 版）です。

## 操作を流し込む —— `--script`

窓のないグラフィックアプリには「いま」がありません。
画面比較は**操作の並び**を両側で同じに再生できて初めて成り立つので、
走らせ方をスクリプトで書けるようにしてあります。

```
# scripts/startup.txt
wait 20000000        # 命令数。秒ではない
mouse 300 200
click L
key esc
type ABC
shot tmp/01.png
```

| | |
|---|---|
| `wait N` / `run N` | N 命令ぶん走らせる |
| `mouse X Y` | カーソルを動かす（モード 12h では画素座標そのもの） |
| `down L\|R\|M` / `up ...` / `click ...` | ボタン |
| `key NAME` | `enter` `esc` `tab` `bs` `space` `up` `down` `left` `right` `home` `end` `pgup` `pgdn` `ins` `del` `f1`〜`f12` `xfer` `nfer`、1 文字、`xHH` |
| `type TEXT` | 行の残りを 1 文字ずつ |
| `shot PATH` | いま画面を PNG に |
| `dump SEG:OFF N` | ゲストのメモリを N バイト（既定 16）。変数の中身を見る |
| `end` | そこで止める |

**時計を命令数にしてあるのは意図的です。** 実時間だとマシンの混み具合で
2 つの走行の位置がずれるし、合わせる先がありません。

## 次にやること

### 1. 画面の突き合わせ（これが目的）

両側に必要なものは揃いました。

* **同じ字形** —— `--font-ank` / `--font-kanji` で移植側と同じ `font/*.FNT`
* **同じ色** —— こちらは `INT 10h AX=1010h` を実装済み、移植側は
  `jw_view_palette()` で `JW_PAL.DAT` を読む。6 ビット → 8 ビットの
  変換規則も揃えてあります（`(v<<2)|(v>>4)`）
* **同じ操作** —— `--script` の並びを両側で再生する

**そして回り始めました。** 移植は原典と同じ場所に、同じ色で、同じ画素に
線を置きます（SAMPLE1 で 7,388 画素一致、差 1.37%）。
`shot` の名前を `.raw` にすると 640×480 の RGBA で書くので、
向こうの `tests/compare.exe` がそのまま差分の地図を出します。

決め方はどれも**こちらの画面から測る**でした。

* 座標変換 —— `DOSEMU_BP=0EFF:17BB DOSEMU_BPPTR=2,3,4,5` で、本物が
  線ごとに計算した画面座標（float）を出し、`.JWC` の同じ線と突き合わせる。
  答えは `screen_x = (int)(x + 121)`、`screen_y = (int)(463 - y)`、**倍率 1**
* ペン → 色 —— 線に沿って画面の色を拾って集計する
  （`jwcad_dos_wasm/tools/measure.py` がやります）。`.JWC` の 2 バイトは
  「線種、ペン」の順で、移植は逆に読んでいた
* 文字 —— **原典は字形を描かず、枠の矩形を描いていた**

残っていることは向こうの RESUME.md に並べてあります
（文字の枠の高さ、レイヤの表示・非表示、本物の UI）。

### 2. WASM 版

本家 dos_emu_cpp と同じ形で。

### 3. EMS（`INT 67h`）

いまは実装していません。JW_CAD は EMS が無ければ conventional メモリで
動くので図面は読めていますが、ファイル選択が「65,535 bytes free」と
言うのはそのせいです。大きい図面を開くときに効いてきます。

### 4. オーバーフローフラグの直しを本家へ

`src/cpu.cpp` のシフト・ローテートが OF を立てていなかったのは
[dos_emu_cpp](https://github.com/yomei-o/dos_emu_cpp) から引き継いだ
コードです。**向こうにも同じ穴があります。**
ソフトウェア浮動小数点を使うプログラムは軒並み踏むので、持ち帰ること。

## 刺された罠

**フラグを「誰も見ていないだろう」で省かない。** シフトとローテートが
OF を一切立てていなかった。踏むのはこの一行です。

```asm
    shl dx, 1
    rcl bx, 1
    jno -            ; 最上位ビットが立つまで回す
```

仮数の正規化はこう書くのが定石で、Microsoft C のソフトウェア浮動小数点
パッケージもそう書いてある。OF が立たないとループが正しい位置で止まらず、
**整数から浮動小数点への変換がいつでも 1.0 になる**。JW_CAD では
それがクリップ枠（DGROUP 0xb5aa/0xb5b2/0xb60e/0xb612 に浮動小数点で入る）を
点 (1.0, 1.0) にして、762 本の線を全部捨てていた——読み込みは成功し、
実体ループは最後まで回り、画面は真っ黒。**どこにも「失敗した」と出ない。**

ローテートが SF/ZF/PF を書き換えていたのも同じ穴の裏側で、
これを触っていいのはシフトだけ。ローテートで書き換えると、呼び出し元が
直前にやった比較の結果が消える。

**「暴走している」を信じない。まず最後まで走らせる。** 前回は
16 万命令でフォント取得の far call がゼロを実行している、と書いてあった。
実際には**660 万命令まで走りきって**、`INT 33h` 機能 0（リセット）が
AX=0 を返したので「ﾏｳｽﾄﾞﾗｲﾊﾞｰが組み込まれていません」と出して
`exit(1)` していた。JW_CAD はマウスの有無を訊いているのではなく、
**要求している**。オーバーレイ 8 のリンク `2ab8:3132`。

**「文字が待っている」と嘘をつかない。** `INT 21h AH=0Bh` が
0xFF 固定だった。JW_CAD のキー待ちは AH=0Bh が「はい」と言ったときだけ
AH=08h で読むので、いつでも「はい」だと永久に読み続ける。
何も打ち込まない走行では 700 万命令のところで画面が止まり、
そのあと 6000 万命令走らせても 1 画素も変わらなかった。
`INT 16h AH=01h` で同じ嘘を避けてあったのに、DOS 側が残っていた。
**入力の「待っている？」は全部同じ `input_ready` フックに繋ぐこと。**

**パレットを受けて無視しない。** JW_CAD は `JW_PAL.DAT` を読んで
`INT 10h AX=1010h` で DAC 16 本、`AX=1000h` でアトリビュートパレットを
恒等に設定する。無視すると絵は合っているのに色だけ EGA の既定になり、
**画面比較としては「違う絵」**になる。ポート書き込みを全部並べて
（`DOSEMU_IO_TRACE=1`）0x3CE/0x3CF しか無いと分かったのが手がかりだった。

**BIOS データエリアを空のままにしない。** 中立ではありません。
JW_CAD は画面の横幅を `0040:004A`、文字セルの高さを `0040:0085` から取って
**割り算します**。0 のままだと `R6003 - integer divide by 0` で、
何も描かずに止まります。しかも Microsoft C のランタイムが出すメッセージなので、
`DIV` 命令を捕まえても引っかかりません。

**`INT 10h` を「受けて無視」にしない。** `AH=0Fh` でモードを取って
終了時に戻すプログラムは多く、返り値が無いと変な値を覚えます。

**DOS/V のプログラムは字形を持っていない。** `INT 15h AX=5000h` で
「字形を写すルーチン」のアドレスを尋ね、それを far call します。
実装が無いと関数ポインタがゴミのままで、最初の 1 文字で落ちます。
これは **`cd 15` をバイト列で探しても見つかりません**——DOS/V のプログラムは
たいてい `int86()` 経由で呼ぶので、機械語に `INT` 命令が現れないからです。

**低位メモリに置いたものは消える前提で。** `INT 15h AX=5000h` が返す
far ポインタの先に 3 バイトのスタブを置いたが、`0090:0000` では
（起動時に書いても、`INT 15h` の直前に書き直しても）ゲストが使うころには
ゼロになっていた。ゼロは `add [bx+si], al` なので、CPU は静かに
前へ進み続ける。いまは `F000:FF00`（ROM BIOS 領域）に置いてある。

**フォントは外から渡せるようにしておく。** 移植の検証に使うなら、
**両方がまったく同じ字形を描かなければ比較に意味がありません。**
だから `--font-ank` / `--font-kanji` で FONTX2 を渡す形にしてあります。

## 使えるトレース

```sh
DOSEMU_DOS_TRACE=1      # INT 21h を 1 本ずつ（開いたファイル名も出る）
DOSEMU_TRACE=lo-hi      # 命令番号の範囲で 1 命令ずつ（2 万命令で数分。範囲は狭く）
DOSEMU_SAMPLE=n         # n 命令ごとに CS:IP。「どこにいるか」を数秒で
DOSEMU_WATCH=lo-hi      # 物理番地への読み書き（VRAM は見ない。**標準出力**に出る）
DOSEMU_BP=seg:off[,..]  # その番地に来るたびにスタック 10 語 ＝ C の引数表
DOSEMU_BPSTR=N          # そのうち N 番目を DS 相対の文字列として出す
DOSEMU_FONT_TRACE=1     # 字形要求（文字コードと格納先）
DOSEMU_IO_TRACE=1       # ポート書き込みと INT 10h
DOSEMU_MOUSE_TRACE=1    # INT 33h
DOSEMU_VRAM_MAP=1       # 各プレーンの立っているビットが見えるページの中か外か
DOSEMU_FPU_TRACE=1      # x87 エスケープ（JW_CAD では 0 回）
```

**グラフィックスを追うときに要るのは「どこにいるか」でも
「何を計算したか」でもなく「誰がどの座標で呼んだか」です。**
`DOSEMU_BP` はそのために足しました。たとえば文字列描画なら:

```sh
DOSEMU_BP=0EFF:23C5 DOSEMU_BPSTR=2 ./dosemu ... 2>/dev/null | grep '^\[bp\]'
# -> args <戻り番地> <文字列> <桁> <行> <色> <モード> ... "その文字列"
```

## 押さえた番地（JW_CAD 側）

ロード基底は `0x110` です（DGROUP が `3485`、これは
[jwcad_dos_wasm](https://github.com/yomei-o/jwcad_dos_wasm) の
リンク時アドレス `3375` ＋ `0x110`）。トレースを読むときの換算に使います。
**`jwcad_dos_wasm/tools/disasm.py` はリンク時アドレスを取るので、
エミュレータで見た CS から `0x110` を引いてから渡してください。**

| エミュレータの CS | リンク | 中身 |
|---|---|---|
| `0110` | `0000` | `main` のある最初のコードセグメント |
| `0995` | `0885` | ステータス行・メニューまわり |
| `0EFF` | `0def` | コンソール層（`0def:23c5` 文字列、`0def:1460` 矩形、`0def:12e8` クリップ枠、`0def:17bb` クリップつきの線） |
| `11B9` | `10a9` | 描画（`10a9:03e1` が 1 文字、`10a9:07dc` が線、`10a9:0732` が色） |
| `1302` | `11f2` | 再描画（`11f2:27eb` が図面の描き直し、`11f2:0680` が実体ごとの表示判定） |
| `23C2` | `22b2` | Microsoft C のランタイム（`22b2:247a` が `int86`、`22b2:70xx` が浮動小数点） |
| `2B85` | `2a75` | Shift-JIS / JIS 変換 |
| `2BC8` | `2ab8` | オーバーレイが載る穴 |
| `3485` | `3375` | DGROUP |

JW_CAD 側の構造・オーバーレイ・ファイル形式は
[jwcad_dos_wasm の RESUME.md](https://github.com/yomei-o/jwcad_dos_wasm/blob/main/RESUME.md)
に全部書いてあります。
